```
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/zyl-mwy/troubleShootingCode.git
git push -u origin main
```

```
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/zyl-mwy/troubleShootingCode.git
git push -u origin main
```

```
git remote add origin https://github.com/zyl-mwy/troubleShootingCode.git
git branch -M main
git push -u origin main
```

```
git config --global http.timeout 300
git remote set-url origin git@github.com:username/repo.git   username和repo.git分别为你的用户名和仓库名称。
git remote set-url origin git@github.com:zyl-mwy/hyperspectralCode.git
```